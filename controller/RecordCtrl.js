'use strict';

/**
 * @ngdoc function
 * @name mapp.controller:RecordCtrl
 * @description
 * # RecordCtrl
 * Controller of the app
 */
angular.module('app')
  .controller('RecordCtrl', function ($scope) {
    $scope.pageclass = "record-page"
  });
